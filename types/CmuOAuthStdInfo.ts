export type CmuOAuthStdInfoDTO = {
	studentId: string; // Mapped from `student_id`
	prenameId: string; // Mapped from `prename_id`
	prenameTH: string; // Mapped from `prename_TH`
	prenameEN: string; // Mapped from `prename_EN`
	firstNameTH: string; // Mapped from `first_name_TH`
	lastNameTH: string; // Mapped from `last_name_TH`
	firstNameEN: string; // Mapped from `first_name_EN`
	lastNameEN: string; // Mapped from `last_name_EN`
	facultyMisId: string; // Mapped from `faculty_mis_id`
	facultyCode: string; // Mapped from `faculty_code`
	facultyNameTH: string; // Mapped from `faculty_name_TH`
	facultyNameEN: string; // Mapped from `faculty_name_EN`
	departmentId: number; // Mapped from `department_id`
	departmentNameTH: string; // Mapped from `department_name_TH`
	departmentNameEN: string; // Mapped from `department_name_EN`
	statusId: number; // Mapped from `status_id`
	statusName: string; // Mapped from `status_name`
	cmuitAccount: string; // Mapped from `cmuitaccount`
};
